<div class="row">
  <div class="columns small-12">
    <ul class='inline-list'>
      {{#each day in days}}
        <li>
          {{day.title}}
          {{day.icon}}
          <img {{bind-attr src=day.icon_url}}>
        </li>
      {{/each}}
    </ul>
  </div>
</div>
